// Phase 4 ui-research-citation — node half: minimal Cordis plugin so the
// package becomes a loader entry; the browser half drives the chat node.
export const name = 'ui-research-citation'

export function apply() {}
