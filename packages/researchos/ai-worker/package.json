{
  "name": "@researchos/dsh-research-ai-worker",
  "description": "Phase 5 research-ai-worker: ResearchOS AI pipeline inside DSH (PDF parse + section chunk + embedding via unified gateway + SQLite vector write + Paper Card + review generation + SQLite status updates). Zero external DB — everything lives in ~/.researchos/data/researchos.db.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": { "default": "./index.js" },
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" }
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "*"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "pdf-parse": "^1.1.1"
  }
}
