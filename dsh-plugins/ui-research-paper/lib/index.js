// Phase 4 ui-research-paper — node half: minimal Cordis plugin so the package
// becomes a loader entry; the browser half (lib/client.js) drives the chat node.
export const name = 'ui-research-paper'

export function apply() {}
