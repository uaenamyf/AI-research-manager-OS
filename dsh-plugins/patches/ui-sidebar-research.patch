--- /tmp/sidebar-orig/slots.ts	2026-08-18 14:16:34
+++ contract/slots.ts	2026-08-18 14:17:01
@@ -23,6 +23,13 @@
      */
     'sidebar.workspaces': { kind: 'single'; scope: 'root'; owner: SidebarSectionOwnerProps }
     /**
+     * The research workspace region: the ResearchOS browsing region seated
+     * parallel to the workspace browser, switched by the sidebar's section
+     * control. Declared by this package's 'sidebar' entry;
+     * @researchos/ui-research-workspace registers the region.
+     */
+    'sidebar.research': { kind: 'single'; scope: 'root'; owner: SidebarSectionOwnerProps }
+    /**
      * The settings seat at the sidebar foot. Declared by this package's
      * 'sidebar' entry; ui-settings registers its trigger row + modal panel.
      * The sidebar passes only its column state — it holds no settings state.
@@ -85,5 +92,5 @@
  */
 export type SidebarRootComponentProps =
   PropsRuntime<'sidebar'>
-  & PropsRenderSlots<'sidebar.workspaces' | 'sidebar.settings' | 'sidebar.footer.action'>
+  & PropsRenderSlots<'sidebar.workspaces' | 'sidebar.research' | 'sidebar.settings' | 'sidebar.footer.action'>
   & SidebarRootInjected & PropsLocale<'sidebar'>
--- /tmp/sidebar-orig/SidebarRoot.tsx	2026-08-18 14:16:34
+++ SidebarRoot.tsx	2026-08-18 14:30:05
@@ -88,6 +88,11 @@
     window.clearTimeout(lingerTimer.current)
     lingerTimer.current = undefined
   }
+  // Section switcher (ResearchOS fusion): the browsing region hosts either the
+  // workspace browser or the research workspace, side by side as two top-level
+  // sections of the left column. State is local: the active section survives
+  // collapse/expand but not a full reload (a reload restores Workspace).
+  const [section, setSection] = useState<'workspaces' | 'research'>('workspaces')
   // Leaving is decided by the column's BOX, not by DOM containment, and only
   // while the bars are drawn. ui-settings renders its full-viewport panel as a
   // fixed-position DESCENDANT of this column, so a pointer moved onto that
@@ -169,13 +174,24 @@
         </button>
       </Tooltip>
 
-      {/* The browsing region fills the column between the controls and the
-          foot in both states; its rail icon column rides the same slot. */}
+      {/* Browsing region (ResearchOS fusion): the workspace browser and the
+          research workspace stack VERTICALLY inside the same column — 工作区
+          upper, 研究区 lower, split by a hairline. Both slots receive the
+          shell's wide flag and the expand request. */}
       <div className={css.regionArea}>
-        {renderSlot('sidebar.workspaces', {
-          wide,
-          expandSidebar: () => { if (collapsed) toggleSidebar() },
-        })}
+        <div className={css.regionUpper}>
+          {renderSlot('sidebar.workspaces', {
+            wide,
+            expandSidebar: () => { if (collapsed) toggleSidebar() },
+          })}
+        </div>
+        <div className={css.regionDivider} />
+        <div className={css.regionLower}>
+          {renderSlot('sidebar.research', {
+            wide,
+            expandSidebar: () => { if (collapsed) toggleSidebar() },
+          })}
+        </div>
       </div>
 
       {/* Footer actions stack above Settings in both sidebar widths. */}
--- /tmp/sidebar-orig/SidebarRoot.module.css	2026-08-18 14:16:34
+++ SidebarRoot.module.css	2026-08-18 14:30:47
@@ -219,8 +219,26 @@
 
 .collapsed .newSessionLabel {
   max-width: 0;
+}
+
+/* ResearchOS fusion: the browsing region stacks the workspace browser
+   (upper) and the research workspace (lower) vertically inside the column. */
+.regionUpper,
+.regionLower {
+  flex: 1;
+  min-height: 0;
+  display: flex;
+  flex-direction: column;
+  overflow: hidden;
 }
 
+.regionDivider {
+  flex: none;
+  height: 1px;
+  margin: 0 8px;
+  background: var(--dsw-alias-border-l2);
+}
+
 /* Region seat: always mounted so the foot never moves. Its trailing margin
    cancels the wide shell inset so the nested scrollbar can sit at the sidebar
    edge; the browser restores that inset inside its own rows. */
