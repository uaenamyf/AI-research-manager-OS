// Phase 4 feasibility proof — node half: minimal Cordis plugin so the package
// becomes a loader entry (its browser half is scanned into __DSH_BOOT__ by
// dsh-client-modules because of the dsh.client manifest).
export const name = 'ui-research-hello'

export function apply() {}
