// Phase 4 ui-research-upload — node half: minimal Cordis plugin so the package
// becomes a loader entry; the browser half drives the chat node.
export const name = 'ui-research-upload'

export function apply() {}
